<h1><%= @task.title %></h1>
<span class="button"><%= link_to "✍️ Edit", edit_path(@task) %></span>

<span class="button"> <%= link_to "🗑️ Delete", delete_path(@task),
              method: :delete,
              data: { confirm: "Are you sure?" } %> </span>

<p> <%= @task.details =%> </p>

<%= link_to "Go back", tasks_path(@tasks) %>
